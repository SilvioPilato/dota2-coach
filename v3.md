You are a senior product engineer helping structure development work into epics and tasks for a project management tool called Beads.
Context
We are building Dota Coach, a personal Dota 2 coaching tool. It analyzes match replays and delivers role-aware, percentile-based performance feedback via a FastAPI backend and a single-page web UI.
The current version (v2) is in progress. We are now specifying the work for v3, which adds two backend features and a full UX overhaul of the web frontend.

What to generate
Create epics and tasks for the following scope of work. Each epic groups a coherent unit of deliverable functionality. Tasks within an epic are concrete, independently implementable work items — not sub-headings.
Rules for tasks:

Each task must be implementable in a single focused session (2–6 hours)
Each task must have a clear, testable done condition
Tasks that touch the same file should be in the same epic where possible
If a task has a hard dependency on another task, note it as "Requires: [task name]"
Do not create tasks for things already implemented in v1/v1.5/v2


Epics and tasks to generate
Epic 1 — Causal death tagging
Add DeathCause classification to laning-phase deaths using rule-based inference on existing odota parser output. No new external data sources required.
Tasks to cover:

Add DeathCause enum and update DeathEvent model in models.py
Implement _classify_death() in extractor.py with the following priority order: TEAMFIGHT → DIVE → GANK_RUNE → NO_TP_RESPONSE → OVEREXTENSION → UNKNOWN. Inputs: death position (x/y), death timestamp, purchase log (for TP cooldown), teamfights list, tower coordinate constants
Add hardcoded tower coordinate constants in extractor.py (Radiant + Dire T1/T2 positions — values to be verified against a real parsed match, marked as TODO)
Wire _classify_death() into the main extraction pipeline so all DeathEvent objects in MatchMetrics are populated with cause and cause_detail
Update prompt.py to render the expanded death_detail block: 4:12 — overextension (mid missing ~40s) instead of just the timestamp
Add two new error rules in detector.py: D1 (rune-timing gank vulnerability: deaths where cause == GANK_RUNE >= 2 → high) and D2 (overextension pattern: deaths where cause == OVEREXTENSION >= 2 → high)
Write unit tests in tests/test_death_classifier.py covering all 6 DeathCause values plus edge cases (TP on cooldown, death inside teamfight window, death near tower)
Spike task: parse fixtures/sample_match.json, locate tower kill coordinates in the objectives array, verify against hardcoded constants and update if needed

Epic 2 — Hero-aware item path validation
Replace the universal first_core_item_minute > 18 rule with a per-hero, per-path system that eliminates false positives (e.g. Radiance Medusa at 22 min is correct) and catches real mistakes invisible to the universal rule.
Tasks to cover:

Add ItemPath dataclass and HERO_ITEM_PATHS lookup table in detector.py with initial coverage for 10 heroes: Anti-Mage, Juggernaut, Phantom Assassin, Drow Ranger, Terrorblade, Medusa, Faceless Void, Wraith King, Luna, Slark
Implement detect_item_path(hero, purchase_log) → ItemPath | None in detector.py: find best-matching path by set overlap; return None if zero overlap (non-standard build → skip rule)
Replace rule #4 in the error detector: use matched_path.first_item_target_minutes if a path matched, else fall back to universal 18-min threshold. Implement overshoot-based severity: > 5 min late → critical, > 2 min late → high, otherwise medium
Update prompt.py first-core-item line to include matched path name: Battle Fury at 19.2 min (target: <18 min for BF standard path — 1.2 min late)
Write unit tests in tests/test_item_paths.py covering: path matching, zero-overlap fallback, universal fallback for unknown hero, overshoot severity thresholds

Epic 3 — Split-pane layout
Restructure static/index.html and static/style.css from a single vertical scroll flow to a fixed three-column layout: history sidebar (220px) | report pane (flex: 1) | chat pane (360px). No scroll on body — each panel scrolls independently.
Tasks to cover:

Restructure index.html DOM: introduce app-shell, top-bar, workspace, sidebar-history, report-pane, chat-pane wrapper elements. Move existing report content into report-pane. Move chat panel out of .report into chat-pane. All existing JS logic remains unchanged.
Write style.css layout rules: app-shell as full-height flex column; workspace as flex row with overflow: hidden and min-height: 0; report-pane with flex: 1 and overflow-y: auto; chat-pane as fixed-width flex column with internal scroll on .chat-messages; top-bar fixed height
Add responsive breakpoints: < 1100px chat-pane becomes bottom drawer with toggle button; < 768px sidebar hides with hamburger toggle

Epic 4 — Progress pipeline stepper
Replace the generic spinner with a 5-step SSE-driven stepper that shows live phase status and elapsed time. Requires converting /analyze from JSON response to SSE stream.
Tasks to cover:

Add sse-starlette to pyproject.toml dependencies
Modify /analyze endpoint in api.py to return EventSourceResponse. Emit event: step with {phase, status, detail?, elapsed_ms?} at the start and end of each of the 5 phases (metadata, download, parse, enrich, llm). Emit event: report with the full MatchReport JSON as the final event. Emit event: step with status: error and message on any exception, then return.
Update the frontend SSE client in index.html: replace the fetch + res.json() call with an EventSource (or fetch + ReadableStream) that processes event: step to update stepper state and event: report to trigger renderReport()
Build the stepper UI component in index.html + style.css: 5 step rows each with icon (pending/spinner/checkmark/error), label, detail text, elapsed time. Stepper collapses with animation when report arrives; compact timing summary persists below the report header.

Epic 5 — Steam ID memory + unified recent matches sidebar
Surface OpenDota recent matches in the sidebar alongside already-analyzed matches, with three distinct states (analyzed / ready / expired). Persist Steam ID across sessions.
Tasks to cover:

Add Steam ID memory in index.html: on page load read localStorage['dota_coach_account_id'] and pre-populate the player ID field; after successful analysis write account_id to localStorage; show dismissable "Last account: [id]" label below the input
Add get_analyzed_ids(account_id) → set[int] to history.py: simple SELECT match_id FROM matches WHERE account_id = ?
Add GET /history endpoint in api.py: fetch last 20 matches from OpenDota GET /players/{account_id}/recentMatches, join with get_analyzed_ids() result, compute replay_available from start_time age, return list[MatchSummary]
Build sidebar UI in index.html + style.css: match list with hero initials avatar, hero name, date/duration, W/L badge, and colored state dot (blue = analyzed, gray = ready, orange+dimmed = expired). On hover: replace W/L badge with contextual action button ("Analyze" or "Re-analyze"). Expired entries are non-interactive.
Wire sidebar interactions: clicking an analyzed entry loads the cached report from localStorage; clicking "Analyze" pre-fills the match ID field and triggers runAnalysis(); clicking "Re-analyze" calls runAnalysis(null, {force_reanalyze: true})
Auto-load sidebar on page load if localStorage has a saved account_id; show loading state during fetch; handle empty state ("No recent matches found") and error state ("Could not load matches — check Player ID")

Epic 6 — Chat panel redesign + timeline click-to-quote
Redesign chat bubbles, add streaming cursor, switch input to auto-grow textarea, and add click-to-quote from the match timeline.
Tasks to cover:

Redesign chat bubbles in index.html + style.css: user bubbles right-aligned with accent color, bottom-right radius 3px; assistant bubbles left-aligned with surface color, bottom-left radius 3px; max-width 85%; proper markdown rendering already in place via marked.js
Add streaming cursor: append <span class="cursor">▋</span> while tokens arrive, remove on stream end; add CSS blink animation for the cursor
Replace <input type="text"> with <textarea> in the chat input bar: auto-resize on input (1 row default, max 4 rows); Enter sends, Shift+Enter inserts newline; disable textarea + send button while streaming
Add quote preview UI above the textarea: hidden by default; shows 📌 [event text] with an × dismiss button when a timeline event is cited; cleared on send
Render the match timeline in report-pane below the priority focus block: chronological list from build_timeline() output (already returned in MatchReport). Each row: timestamp (monospace, tabular), event description (deaths highlighted in red), cite button (→ icon, visible on row hover)
Wire click-to-quote: cite button click → set currentQuote, show quote preview, briefly highlight the cited row (2s background fade), focus textarea
Update prompt.py build_user_message() to accept optional quote: str | None parameter and prepend [Referenced event: {quote}]\n\n when present; update the /chat frontend call to include quote in the request body; update ChatRequest model in models.py to include quote: str | None = None


Output format
For each epic, output:
Epic [N]: [Title]
One sentence describing the user value delivered by this epic.
Then list tasks as:

[Task title] — [one sentence done condition]. [Requires: X] if applicable.

Keep task titles short and action-oriented (verb + noun). Do not repeat information from the context section in the task descriptions.